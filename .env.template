# Database root user credentials
ROOTUSER=<your-root-user>
ROOTPASS=<your-root-password>

# Database name
DATABASE=<your-database-name>

# Application-specific MySQL user credentials
MYSQL_USERNAME=<your-mysql-username>
MYSQL_USERPASS=<your-mysql-user-password>

# Example usage instructions:
# Replace <your-root-user> with the actual root username.
# Replace <your-root-password> with the actual root password.
# Replace <your-database-name> with the actual database name.
# Replace <your-mysql-username> with the actual MySQL username.
# Replace <your-mysql-user-password> with the actual MySQL user password.
